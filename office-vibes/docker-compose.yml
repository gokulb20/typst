version: '3.8'

services:
  office-vibes:
    build: .
    container_name: office-vibes
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - GROK_API_KEY=${GROK_API_KEY}
      - NODE_ENV=production
    volumes:
      - /tmp/office-vibes:/tmp/vibe-docs
    restart: unless-stopped
