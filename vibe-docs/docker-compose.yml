version: '3.8'

services:
  vibe-docs:
    build: .
    container_name: vibe-docs
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - GROK_API_KEY=${GROK_API_KEY}
      - NODE_ENV=production
    volumes:
      - /tmp/vibe-docs:/tmp/vibe-docs
    restart: unless-stopped
